package drivers;

/**
 * Метка драйвера, который поднимает сессию в облаке BrowserStack.
 * <p>
 * По ней тестовая база понимает, что у сессии есть видео и его можно приложить
 * к отчёту: на локальных стендах записывать нечего.
 * <p>
 * Признак висит на драйвере, а не проверяется по {@code deviceHost}, потому что видео —
 * свойство поднятой сессии, а не ключа запуска. Тестовой базе так вообще не нужно знать
 * про стенды: она спрашивает тот драйвер, который сама же и выбрала. Ответ остаётся
 * верным и когда драйвер выбран в обход ключа — например, у iOS-тестов.
 */
public interface BrowserstackStand {
}
